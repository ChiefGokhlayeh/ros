# Autonomous Systems Design Laboratory Docker Image

This repository defines the Docker image used in ASD lab. The image created here is used as base-image for devcontainers in `asd-docker-template`.
